<?xml version="1.0" encoding="utf-8"?>
<manifest package="es.iessaladillo.pedrojoya.pr05"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.profile.FragmentProfile"
            android:label="@string/name_Profile"
            android:windowSoftInputMode="adjustPan">

        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:label="MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.avatar.FragmentAvatarActivity"
            android:label="@string/avatar_label"
            android:parentActivityName=".ui.profile.FragmentProfile"/>
        <activity
            android:name=".ui.mainViewCard.FragmentViewCard"
            android:label="@string/name_MainViewCard">
        </activity>
    </application>

</manifest>